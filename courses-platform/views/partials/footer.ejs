<footer class="bg-gray-800 border-t border-gray-700 mt-auto">
    <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col md:flex-row justify-between items-center">
            <div class="text-gray-400 text-sm">
                <p>Security Research Courses Platform</p>
                <p class="mt-1">Educational purposes only</p>
            </div>

            <div class="mt-4 md:mt-0 flex items-center space-x-6">
                <a href="/courses" class="text-gray-400 hover:text-white text-sm">Courses</a>
                <% if (typeof currentUser !== 'undefined' && currentUser) { %>
                    <a href="/dashboard" class="text-gray-400 hover:text-white text-sm">Dashboard</a>
                <% } %>
            </div>
        </div>

        <div class="mt-4 pt-4 border-t border-gray-700 text-center text-gray-500 text-xs">
            <p>&copy; <%= new Date().getFullYear() %> Security Research Platform. For educational use.</p>
        </div>
    </div>
</footer>
